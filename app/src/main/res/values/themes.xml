<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoanAppAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LoanAppAndroid" parent="Base.Theme.LoanAppAndroid" />

    <style name="Theme.MyApplication" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="AppThemeLight.Fullscreen" parent="AppThemeLight">
        <item name="android:actionBarStyle">@style/Widget.AppThemeLight.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:fontFamily">@fonts/custom_font_family</item> <!-- For devices using AppCompat -->
        <item name="fontFamily">@fonts/custom_font_family</item>
    </style>

    <style name="ThemeOverlay.LoanAppAndroid.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:fontFamily">@fonts/custom_font_family</item> <!-- For devices using AppCompat -->
        <item name="fontFamily">@fonts/custom_font_family</item>
    </style>
</resources>